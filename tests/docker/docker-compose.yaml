services:
    app:
        container_name: wishlist
        image: wishlist:test
        build:
            context: ../../
        network_mode: host
        environment:
            ORIGIN: http://localhost:3280
            TEST_API_KEY: ${TEST_API_KEY}